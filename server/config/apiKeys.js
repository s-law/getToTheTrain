// BART API key shown here is default public key available
// to anyone without needing to register. 511 does not
// have a public key available

module.exports = {
  bart: process.env.BARTKEY || 'MW9S-E7SL-26DU-VV8V',
  caltrain: process.env.CALTRAINKEY
};